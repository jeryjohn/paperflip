name: flip_book
description: Realistic book page-flip widget with drag-to-curl animation, programmatic control, and PDF support.
version: 0.1.0
homepage: https://github.com/yourorg/flip_book

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  pdfrx: ^2.4.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
