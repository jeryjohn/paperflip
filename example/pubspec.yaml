name: flip_book_example
description: Example app for the flip_book package.
publish_to: none
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  flip_book:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
