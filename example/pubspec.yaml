name: flip_book_example
description: Example app for the flip_book package.
publish_to: none
version: 0.1.0

environment:
  sdk: '^3.10.0'
  flutter: '>=3.41.0'

dependencies:
  flip_book:
    path: ../
  flutter:
    sdk: flutter
  pdfrx: ^2.4.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

